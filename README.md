# arrels-web

The Arrels website, developed incrementally from OpenProject tasks through the pixel-office workflow.

The implementation is intentionally task-driven. See [the OpenProject task guide](docs/OPENPROJECT_TASKS.md) before creating website work packages.
